<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ivory Llaves</title>
    <link rel="icon" type="images/x-icon" href="/public/img/isotipo con fondo.png"/>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    <!-- Montserrat Font Family -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <!-- Propietary CSS -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/users/key_actions.css">

    <!-- Propietary Scripts -->
    <script src="/scripts/search_key.js" defer></script>
</head>
<body>
    <div class="d-flex">
        <a href="/users" class="btn-back mt-3 ms-2 montserrat-rg"><i class="fa fa-chevron-left p-2"></i>Atrás</a>
        <img src="/img/logotipo-1.png" class="mx-auto my-5 p-3 ivory-logo" alt="Ivory Homes">
    </div>

    <div class="container p-5">
        <h1 class="montserrat-bd p-4 my-5 text-center">Devolver una llave</h1>
        <form action=<%= action %> method="post">
            <div class="row my-3 mx-auto">
                <span class="form-label h5">Nombre del Apartamento:</span>
                <div class="search-box">
                    <div class="d-flex">
                        <input id="search" class="col form-control" type="search" placeholder="Buscar" name="apartment">
                        <div class="col btn-search"><i class="fa fa-search"></i></div>
                    </div>
                </div>
                <div class="result-box" result-box>
                </div>
            </div>
            <% if (keysets == "") { %>
                <div class="row pt-2 pb-4 mt-4">
                    <button class="col-6 btn mx-auto" type="submit">Buscar</button>
                </div>
            <% } else { %>
                <div class="row mx-auto input-group my-3">
                    <span class="form-label h5">Llave:</span>
                    <div class="dropdown" dropdown>
                        <input type="text" placeholder="Escoja una llave" id="dropdown-toggle" class="form-control dropdown-toggle" data-bs-toggle="dropdown" name="keyset">
                        <ul class="dropdown-menu" dropdown-menu>
                        </ul>
                    </div>
                </div>
                <div class="row pt-2 pb-4 mt-4">
                    <button class="col-6 btn mx-auto" type="submit">Registrar</button>
                </div>
            <% } %>
        </form>
        <% if (message == "success") { %>
            <div class="alert alert-success" role="alert">
                Llave Devuelta!
            </div>
        <% } else if (message == "missing") { %>
            <div class="alert alert-warning" role="alert">
                Por favor rellene todos los campos!
            </div>
        <% } %>
    </div>

    <div id="apt" data-apt="<%= apartments %>"></div>
    <div id="kst" data-kst="<%= keysets %>"></div>
</body>
</html>